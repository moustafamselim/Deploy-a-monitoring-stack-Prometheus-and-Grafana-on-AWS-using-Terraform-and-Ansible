---
# Install Node Exportar 
all:
  hosts:
    control:
      ansible_host: 0.0.0.0  # IP of the target server
      ansible_user: ubuntu         # SSH user for the target server
      ansible_ssh_private_key_file: /path/key
      ansible_python_interpreter: /usr/bin/python3
    
    target:
      ansible_host: 0.0.0.0  # IP of the target server
      ansible_user: ubuntu         # SSH user for the target server
      ansible_ssh_private_key_file: /path/key
      ansible_python_interpreter: /usr/bin/python3



      
